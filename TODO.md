- [x] 블랙리스트, 화이트리스트를 통해서 차단되어있는 서버의 글을 검색을 통해 가져오려고 할 때, 해당 서버가 차단되어있다는 사실을 명시해줘.
  - 검색 시 `@user@domain.com` 형식이면 도메인 차단 여부 확인 → `blocked_domain` 필드로 프론트에 경고 표시
  - `/fetch-post`, `/fetch-actor` URL 불러오기 시에도 도메인 차단 확인 → 구체적인 오류 메시지 반환
- [x] 관리페이지-연합탭 카드 상단에 있는 연합이란 글씨 없애고 블랙리스트 화이트리스트 카드 접게 해줘.
  - "연합" h3 제거, chevron 아이콘으로 접기/펼치기 구현
- [x] 관리 페이지-연합탭 블랙/화이트리스트 설정하는 란 아래에는 지금까지 서버에 들어온 모든 리모트 서버의 목록을 띄워줘.
  - `GET /api/admin/remote-servers` 엔드포인트 추가, 연합 페이지 하단에 접이식 카드로 표시
- [x] 오너의 권한은 어드민+모더레이터야. 모든 권한을 가지고 있어.
  - 백엔드 10곳, 프론트 2곳 permission check에 "owner" 추가
- [x] 블랙리스트에 차단하는 서버 설정시 사유를 기록할 수 있게 해줘.
  - `FederationBlock.reason` 컬럼 추가, 연합 페이지에 사유 입력 필드 및 목록 표시
- [x] 블랙리스트 선택시에는 차단만, 화이트리스트 선택시에는 허용만 등록할 수 있게 해줘.
  - 선택된 모드에 따라 입력 폼과 목록이 차단/허용만 표시
- [x] 메인 브랜치에서 디벨롭 브랜치를 분리하고 프로덕션 브랜치에는 시드 데이터 지워줘. 우리는 디벨롭 브랜치에서 개발을 진행할거야.
  - `develop` 브랜치 생성, `main`은 프로덕션용
- [x] 로그아웃할 때 메인 페이지로 넘어가기 전에 무조건 타임라인 창이 깜빡 스쳐가는데 그렇게 되지 않게 해줘.
  - `router.replace("/")`를 `api.logout()`보다 먼저 호출하여 타임라인 렌더링 방지
- [x] 대표 아이콘, 파비콘, 모바일앱 아이콘은 무조건 정사각형으로 고정해줘.
  - CSS `object-fit: cover`, `width`/`height` 동일값 적용, 도움말에 정사각형 권장 문구 추가
- [x] 파비콘은 무조건 ico 확장자만 된다는 거 명시해줘.
  - 설정 페이지 도움말에 ".ico 확장자만 지원" 문구 추가
- [x] 좌측 사이드바 상단 헤더, 404페이지, / 정보 페이지에 뜨는 대표아이콘도 모서리 둥글게 해줘.
  - `.sidebar-logo-img`, `.not-found-logo-img`, `.home-logo img`에 `border-radius: 50%` 적용
- [x] 로그아웃해도 좌측 사이드바 헤더 대표아이콘 바뀐 걸로 뜨게 해줘.
  - `/api/server-info`에서 불러오므로 로그아웃 후에도 정상 표시 (기존 동작)
- [x] / 페이지 상단 아이콘 변경된 아이콘으로 뜨게 해줘.
  - `/api/server-info`에서 불러오므로 정상 표시 (기존 동작)
- [x] init() 에 들어가는 마이그레이션 코드 다른 파일로 빼서 모듈화.
  - `app/migrations.py`로 분리, `models.py:init_db()`에서 호출
- [x] 모더레이션 로그 페이지 따로 만들어줘. 중재 카테고리로 넣으면 돼.
  - `/admin/moderation-log` 페이지 생성, AdminNav "중재" 섹션에 추가
- [x] 이메일 도메인 차단 목록 페이지도 만들어줘. 이것도 중재 카테고리로 넣어.
  - `/admin/blocked-domains` 페이지 생성, AdminNav "중재" 섹션에 추가
